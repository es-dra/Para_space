# Daily Memory: 2026-05-11

## Entry 1 - Project State Onboarding Snapshot

完成范围：

- 按当前 canonical 文档和活跃代码入口梳理项目整体状态。
- 本轮没有修改训练、分析代码或实验 artifact；只新增这条恢复用 daily memory。
- 使用 `research-coding-workflow`；由于当前运行规则要求用户显式要求 subagent，本轮未启动真实 subagent，改用主流程内模拟独立审查。

核查过的主要文件与目录：

- `doc/current_context.md`
- `doc/project_index.md`
- `doc/research_contract.md`
- `doc/claims_ledger.md`
- `doc/evidence/experiment_registry.md`
- `doc/evidence/run_manifest_stage_b.md`
- `doc/evidence/analysis_manifest_stage_c.md`
- `doc/decisions/README.md`
- `doc/decisions/2026-05-10_return_to_mainline.md`
- `doc/stages/stage_b/*`
- `doc/stages/stage_c/current_status.md`
- `experiments/Phase1_FittingDynamics/README.md`
- `experiments/Phase1_FittingDynamics/analyze_stage_b_controls.py`
- `experiments/config.py`
- `src/trajectory_schema.py`
- `results/FittingDynamics_StageB*`
- `results/FittingDynamics_StageC*`

运行过的命令与结果：

```bash
git status --short --branch
```

结果：`main...origin/main`，已有未提交文档/记忆改动和未跟踪的 `doc/current_context.md`、`doc/commerce/`、`doc/decisions/2026-05-10_return_to_mainline.md`。这些均按已有用户成果处理，未回滚。

```bash
pytest -q
```

结果：`97 passed, 2 warnings in 10.84s`。两个 warning 均来自 `tests/test_alignment.py` 中测试函数返回 `True`，未来 pytest 会要求改成 `assert` 风格。

证据支持的结论：

- 当前主问题仍是 arbitrary-scale INR SR 中单图 fitting/adaptation dynamics 是否存在可测、可解释、非平凡的参数或响应轨迹结构，以及这些结构能否被内容、局部几何、尺度或旋转解释。
- 当前项目可靠产物是 fitting-dynamics probing / failure-audit 工具链和多轮 negative/mixed gate；不是自然图 geometry-response 机制、等变性收益或训练加速证明。
- Stage B 已完成 scratch SIREN / reduced LIIF 平台验证；Stage B-prime、B-response、LIIF internal-unit 和 C-minimal 都没有提供 clean mechanism gate。
- 2026-05-10 的主线决策是收缩到 probing/failure-audit paper framing；若继续机制路线，只允许先设计 controlled mechanism test，不能继续自然图 descriptor/rerank 修补。
- 当前正式 reduced LIIF 证据链使用 `LIIF_CONFIG_REDUCED` scratch SR x4；pretrained LIIF/LIIF-EQ、equivariance comparison、update prediction 和 training acceleration 仍是 blocked。

未解决风险：

- `doc/current_context.md` 和 `doc/decisions/2026-05-10_return_to_mainline.md` 目前是未跟踪文件，但内容已被其他 canonical 文档引用或作为当前恢复入口使用；提交前需要一起纳入版本控制或明确保留为本地草稿。
- `paper/` 仍是历史非 canonical 草稿，不能直接作为当前论文源。
- `doc/commerce/ASISR--I2V.md` 是长期科研-商业蓝图，不是当前实验结论文档；其中设想不能升级为论文 claim。
- alignment 测试 warning 不影响当前测试通过，但应在后续小清理中把返回值改为显式断言。

下一步：

- 若目标是近期论文出口，优先整理 claim-evidence table、paper outline、negative gates 和 artifact reproducibility。
- 若目标是继续机制路线，先写 controlled mechanism test 的 Node Contract、positive/negative controls 和 Metric Validity Gate。
- 若只是工程维护，优先处理未跟踪 canonical 文档的提交状态和 pytest warning，不应重开 Stage C 自然图修补。

## 专家评审

可靠：本轮结论来自当前 canonical 文档、实验 registry、run manifest、active CLI README、结果目录和完整 pytest 健康检查；没有把测试通过误写成科学正进展。

证据不足：本轮没有重跑任何训练或诊断分析，没有独立验证 JSON 数值，也没有核查 pretrained/LIIF-EQ provenance。

下一步是否合理：合理。当前最应该先做路线选择，而不是默认继续实验；如果做机制路线，必须先进入 controlled setting 并冻结 gate。

防止过度宣称：不能把 probing/failure-audit 工具链写成机制发现，不能把 baby/woman 的小正 C-minimal 增量外推到自然图普遍规律，也不能把长期商业蓝图当作当前科研证据。

## Entry 2 - Workflow Correction And Research Reframe Review

完成范围：

- 回应用户对全局 subagent 规则、上一轮 onboarding 合规性、研究方向问题和代码/文档卫生问题的质疑。
- 本轮启动了一个真实只读审查型 subagent，用于检查全局工作流冲突、上一轮缺口、NTK/spectral reframe 和项目清理规则。
- 已将跨项目工作流改进写入 `/home/admin/.codex/AGENTS.md`，新增“项目结构健康与删减治理”规则；本条 memory 只记录项目事实和本轮判断。

核查过的主要证据：

- `/home/admin/.codex/AGENTS.md`
- `doc/current_context.md`
- `doc/research_contract.md`
- `doc/claims_ledger.md`
- `doc/decisions/2026-05-10_return_to_mainline.md`
- `experiments/Phase1_FittingDynamics/README.md`
- `src/models/liif.py`
- 相关文献网页：NTK、Fourier Features、Spectral Bias、LIIF、LMF、IGA、LTE、A-LIIF、meta-learned INR initialization、COIN++ 等。

运行或使用的工具与结果：

- `spawn_agent`：启动真实只读审查型 subagent；审查结论认为 NTK/spectral/global-coupling reframe 比继续 Stage C patch matching 更强，但必须作为新受控诊断路线，不能直接升级机制 claim。
- `sed` / `rg` / `nl`：核查全局规则和 LIIF 代码结构。
- `apply_patch`：更新 `/home/admin/.codex/AGENTS.md`，新增跨项目删减治理规则。

证据支持的判断：

- 全局工作流确实长期授权非简单科研任务默认使用监督/审查型 subagent；上一轮未启动真实 subagent 的正确原因是当前更高优先级运行时工具规则要求用户显式提到 subagent/委托/并行时才允许 `spawn_agent`。上一轮最终说了“当前工具规则不能启动”，但没有充分说明这是运行时规则覆盖长期偏好，也缺少事前 Node Contract 和清楚的 subagent 状态说明。
- 用户提出的 NTK/spectral fitting dynamics reframe 更接近原始主问题，比继续自然图 patch nearest-neighbor / descriptor / rerank 修补更合理。
- LIIF 的 per-cell encoder feature 轨迹不能天然当作局部对象；即使当前 reduced EDSR 的名义 receptive field未必在所有设置下严格全图，12x12 LR 输入和多层卷积/共享参数足以产生强空间耦合，且 Stage B-response 已显示 LIIF encoder/decoder/interaction 不可简单分离。
- SIREN 残差频谱收敛顺序和 LIIF feature trajectory magnitude / modulation-complexity alignment 是更基础、更可验证的候选问题；但新路线必须先冻结指标、controls 和停止条件，避免把 spectral proxy 变成新的 PCA 故事。
- 代码/文档卫生批评成立：当前工作流过度偏向保留/归档，容易让活跃入口被旧脚本、失败路线和重复文档污染。全局规则已补充：退役、删除、canonical 收敛和 cleanup gate 可以是正当交付物。

未解决风险：

- 用户列出的若干 2025 文献需要后续做正式 bib/provenance 核查；本轮只做路线级 sanity check，不形成完整 related work。
- NTK 解释在有限宽、Adam、cosine schedule、非 lazy training 的当前 runs 上只能作为诊断语言，不能直接当作精确定理。
- LIIF global coupling 的强弱需要用 empirical kernel / frozen encoder-decoder / hybrid intervention / controlled input 进一步验证。

下一步：

- 暂停 Stage C natural-image patch matching 修补。
- 若继续科研路线，先写一个新的 Node Contract：`spectral fitting order and LIIF coupling diagnostic`，明确 SIREN residual spectrum、LIIF feature trajectory magnitude、positive/negative controls、平凡 baseline 和停止条件。
- 若先清理工程，做一次 architecture cleanup audit：列出可删除、可退役、需归档和必须保留的入口与 artifact，再运行窄测试和更新入口文档。

## 专家评审

可靠：全局 subagent 授权、运行时覆盖关系和新增删减治理规则都由文件或工具行为直接支持；研究重定位与当前多轮 negative/mixed gate 一致。

证据不足：还没有实现 spectral diagnostic，也没有重算 empirical NTK 或 modulation-complexity 指标；因此不能说新路线已经成立。

下一步是否合理：合理。现在应把新方向先冻结为受控诊断节点，而不是继续修旧 Stage C，也不是直接写成论文主贡献。

防止过度宣称：不能把 NTK/spectral 术语当作理论证明；不能把 LIIF 不局部的直觉替代实证；不能把清理代码误解为删除科研证据，清理必须保留可追溯的 canonical artifacts。

## Entry 3 - Project Structure Cleanup And Rule Hardening

完成范围：

- 按“项目结构健康与删减治理”规则执行一次项目清理，目标是收敛活跃入口、删除 retired/blocked 分支和非 canonical 中间结果。
- 本轮没有进入新实验实现；没有删除 `Data/`、`model/`、`pretrained/`、当前 Stage B raw official runs 或 checkpoint。
- 使用两个真实只读审查型 subagent：
  - cleanup audit：检查 active vs retired 入口、可删结果和反对意见；
  - workflow/rules audit：检查全局规则是否仍过度保留，并建议规则补强。

全局规则更新：

- 修改 `/home/admin/.codex/AGENTS.md`，明确全局 subagent 规则只是用户长期偏好，真实 subagent 是否可创建由当前运行时系统/工具策略决定。
- 补强“脚本与入口规范”：新分析脚本默认临时，只有服务 canonical artifact、被测试覆盖、被文档索引或属于可复现实验协议时才转为持久 CLI。
- 补强“项目结构健康与删减治理”：retired / diagnostic-only / blocked-for-claim / 未引用对象默认删除；归档是例外。
- 补强“结果与产物组织”：非 canonical 分析脚本、派生表格、debug/smoke 输出和一次性中间结果默认可删除。
- 增加 artifact triage：`promote` / `keep-canonical` / `archive-with-index` / `delete`。

项目清理内容：

- 删除 retired Stage C 自然图修补脚本：
  - `analyze_stage_c_geometry_response.py`
  - `analyze_stage_c_failure_audit.py`
  - `analyze_stage_c_response_object_audit.py`
  - `analyze_stage_c_patch_unit_gate.py`
  - `analyze_stage_c_lr_cell_feature_trajectory.py`
- 删除 old controlled self-similarity 脚本：
  - `analyze_stage_c_controlled_self_similarity.py`
  - `generate_controlled_self_similarity_images.py`
- 删除 legacy / blocked 入口：
  - `aggregate_results.py`
  - `viz_trajectory.py`
  - `run_finetune.py`
- 删除 blocked pretrained / LIIF-EQ 实现：
  - `src/models/pretrained_liif.py`
  - `src/models/pretrained_liif_eq.py`
  - `src/models/eq/`
- 删除对应 retired tests：
  - `test_controlled_self_similarity.py`
  - `test_stage_c_failure_audit.py`
  - `test_stage_c_geometry_response.py`
  - `test_stage_c_lr_cell_feature_trajectory.py`
  - `test_stage_c_patch_unit_gate.py`
  - `test_stage_c_response_object_audit.py`
- 删除 ignored generated outputs：
  - `results/FittingDynamics/`
  - `results/FittingDynamics_smoke/`
  - `results/FittingDynamics_StageC_diagnostics/`
  - `results/FittingDynamics_StageC_controlled/`
  - `results/FittingDynamics_StageC_controlled_diagnostics/`
  - `results/FittingDynamics_StageB_diagnostics/stage_c_lr_cell_feature_trajectory_c_minimal_2026-05-10.json`
  - project `__pycache__/`
- 保留：
  - `results/FittingDynamics_StageB/`
  - `results/FittingDynamics_StageB_diagnostics/stage_b_{prime,response,liif_unit}_*.json`
  - Stage B training / validation / audit CLIs。

必要代码修正：

- 将 `analyze_stage_b_response_audit.py` 从旧 Stage C geometry helper 解耦，内联当前 Stage B response audit 所需的 LIIF model build / image path / state unflatten helper。
- 将 `analyze_stage_b_liif_unit_audit.py` 改为复用 Stage B response audit helper，不再依赖已删除 Stage C 脚本。
- 收窄 `src/models/__init__.py`、`src/trajectory_schema.py`、`check_outputs.py`、Stage B controls / trajectory audit 中的 model family 到当前 scratch SIREN / LIIF / LTE。
- 修复 `tests/test_alignment.py` 的 pytest return warning。

文档更新：

- 更新 `experiments/Phase1_FittingDynamics/README.md`、`doc/Refactor_plan.md`、`doc/project_index.md`、`doc/evidence/result_artifact_policy.md`、`doc/evidence/experiment_registry.md`、`doc/evidence/analysis_manifest_stage_c.md` 和相关 decision docs，明确旧 Stage C 结果只保留总结，不再是 active runnable entrypoint。
- 修复 archive 中指向已删除脚本/模块的本地 Markdown 链接。

验证命令与结果：

```bash
pytest -q
```

结果：`43 passed in 13.40s`。

```bash
python - <<'PY'
from pathlib import Path
import re, sys
files=[Path('RESEARCH_PLAN.md')] + sorted(Path('doc').rglob('*.md')) + [
    Path('experiments/Phase1_FittingDynamics/README.md'),
    Path('paper/README.md'),
]
pat=re.compile(r'\[[^\]]+\]\(([^)]+)\)')
broken=[]
for f in files:
    if not f.exists():
        continue
    for m in pat.finditer(f.read_text(encoding='utf-8', errors='replace')):
        target=m.group(1).split('#', 1)[0]
        if not target or '://' in target or target.startswith('mailto:'):
            continue
        p=(f.parent / target).resolve()
        if not p.exists():
            broken.append((str(f), target))
if broken:
    print('BROKEN LINKS')
    for item in broken:
        print(item)
    sys.exit(1)
print('no broken local links')
PY
```

结果：`no broken local links`。

清理后状态：

- `experiments/Phase1_FittingDynamics/`：只剩 9 个文件，目录大小约 `128K`。
- `tests/`：只剩 7 个测试文件，目录大小约 `52K`。
- `src/models/`：只剩 `liif.py`、`lte.py`、`__init__.py`，目录大小约 `32K`。
- `results/`：只剩 `FittingDynamics_StageB/` 和 `FittingDynamics_StageB_diagnostics/`。

反思：

- 这次清理比 subagent 的保守建议更激进；这是有意的。只“标记 retired”不足以解决用户指出的长期结构腐化问题。
- 当前项目已经明显更接近个人维护项目应有的 active surface：少入口、少历史分支、少中间结果、结论集中在 canonical docs。
- 仍保留 Stage B LIIF-unit audit 脚本和 JSON，因为它是当前 Stage B response-object 负结论的一部分；但它有 normalization caveat，未来若不再服务新 spectral/NTK 节点，也应作为下一轮 cleanup 候选。
- `paper/` 仍是 historical draft，`doc/archive/` 仍很重；本轮没有删除 archive，因为它是 tracked historical material。后续若继续结构治理，可以再做“archive shrink / paper reset”节点。

## 专家评审

可靠：清理对象主要是已停止、blocked、legacy 或非 canonical 的脚本/结果；保留了当前 Stage B raw runs、Stage B audit JSON 和通过测试的核心入口。

证据不足：删除 ignored results 后，旧 Stage C 具体 JSON 数值不再可直接本地检查，只能依赖 decision/stage docs 的摘要。这是本轮主动选择的 tradeoff，用可维护性换掉已停止路线的细粒度可追溯性。

下一步是否合理：合理。项目现在适合先写新的 `spectral fitting order / LIIF coupling` Node Contract，或再做一次更小的 archive/paper 清理；不应恢复旧 Stage C 脚本。

防止过度宣称：本轮是工程健康推进，不是科研正结果；测试通过和目录变小不能写成科学进展。旧 Stage C 结论仍是 mixed/negative summary，不能因删除旧脚本而改变 claim 边界。

## Entry 2 - Project Surface Shrink And Spectral Reframe Planning

完成范围：

- 继续执行项目结构健康治理，重点处理第一轮后仍明显污染 active surface 的 `paper/` 和 `doc/archive/`。
- 使用两个只读审查 subagent：
  - 项目健康审查：指出 `paper/` 历史稿、旧 archive 子树和活跃文档直链 archive 是主要残留风险。
  - 科学路线审查：指出 spectral fitting-order reframe 值得尝试，但必须先证明“局部 spectral fitting order”对象本身可测、稳定且超过 controls。

改动文件或产物：

- 删除旧论文正文、sections、figures 和未核查 bib seed，只保留 `paper/README.md` 说明旧稿已删除。
- 删除 `doc/archive/historical_docs/`、`doc/archive/stage_summaries_2026-05-09/` 和 `doc/archive/claude_rules_2026-05-09/`，只保留 `doc/archive/README.md`。
- 更新 active docs，避免直接链接旧 archive 子文件：
  - `RESEARCH_PLAN.md`
  - `doc/README.md`
  - `doc/project_index.md`
  - `doc/current_context.md`
  - `doc/claims_ledger.md`
  - `doc/stages/stage_a/closeout.md`
  - `doc/stages/stage_b/closeout.md`
  - `doc/stages/stage_c/current_status.md`
  - `doc/decisions/2026-05-07_stage_ab_freeze.md`
  - `doc/evidence/analysis_manifest_stage_c.md`
- 继续更新全局工作流 `/home/admin/.codex/AGENTS.md`：
  - 历史论文草稿、旧 bib seed、旧长日志、旧本地工具规则若含 blocked claim 或过时协议，默认删除或缩减为短 README/decision 摘要。
  - 活跃文档默认链接 canonical sources，不直接链接大量旧 archive 子文件。
  - 清理验证应包括 active 文档本地链接、已删除入口引用搜索和缓存检查；append-only memory 不应强迫保留退役文件。

运行过的命令/测试/分析及结果：

```bash
pytest -q
```

结果：`43 passed in 12.12s`。

```bash
python - <<'PY'
from pathlib import Path
import re, sys
files=[Path('RESEARCH_PLAN.md')] + sorted(Path('doc').rglob('*.md')) + [
    Path('experiments/Phase1_FittingDynamics/README.md'),
    Path('paper/README.md'),
]
pat=re.compile(r'\[[^\]]+\]\(([^)]+)\)')
broken=[]
for f in files:
    if not f.exists():
        continue
    text=f.read_text(encoding='utf-8', errors='replace')
    for m in pat.finditer(text):
        target=m.group(1).split('#', 1)[0]
        if not target or '://' in target or target.startswith('mailto:'):
            continue
        p=(f.parent / target).resolve()
        if not p.exists():
            broken.append((str(f), target))
if broken:
    print('BROKEN LINKS')
    for item in broken:
        print(item)
    sys.exit(1)
print('no broken local links')
PY
```

结果：`no broken local links`。

清理后状态：

- `paper/`：约 `8K`，只剩 `README.md`。
- `doc/archive/`：约 `8K`，只剩 `README.md`。
- `experiments/Phase1_FittingDynamics/`：约 `128K`，只保留当前 Stage B run/check/audit 入口。
- `tests/`：约 `52K`，只保留当前 Stage B/schema/alignment 测试。
- `results/`：只剩 `FittingDynamics_StageB/` 和 `FittingDynamics_StageB_diagnostics/`。
- 非 venv `__pycache__` 和 `.pytest_cache` 已清理。

证据支持的结论：

- 当前 active project surface 已基本达到“少入口、少历史分支、少中间结果”的要求。
- 旧 `paper/` 和 `doc/archive/` 不再能被误读为当前论文草稿或当前事实入口。
- 当前 workflow 规则已经覆盖分析脚本、中间结果、旧论文草稿、旧 bib、旧长日志和旧工具规则的默认删减原则。

reframe 规划状态：

- 用户提出的 NTK/spectral reframe 是当前最有科学防守性的方向之一。
- 建议下一正式节点不是重开 Stage C，而是只读 `Stage B-spectral audit`：用现有 Stage B snapshots 测量 residual spectral acquisition order 的全图、局部 patch/LR-cell 和架构内部单位表现。
- 主指标应从全图 low-frequency ratio 转为局部 band acquisition order 的空间组织及其相对 controls 的增量。
- 必须预注册 controls：空间 shuffle/block shuffle、频带标签或 phase/random target control、时间顺序/permutation control、content/bicubic baseline，以及后续必要的 LIIF frozen encoder/decoder ablation。

未解决风险和下一步：

- `Data/`、`model/`、`pretrained/`、ignored `.claude/` 和 `claude-wrapper.sh` 没有删除；前者需要明确 artifact mapping 和授权，后者属于用户本地工具配置而非 active project source。
- daily memory 中历史命令仍可能指向已删除文件；这是 append-only 记录的代价，不应反向要求保留退役代码。
- 下一步若正式推进 reframe，应先写 Node Contract 和 Metric Validity Gate，而不是直接写分析脚本。

## 专家评审

可靠：本轮删除对象均是已阻塞、已总结或高误导风险的历史材料；测试和 active 文档链接均通过；项目入口明显收窄。

证据不足：本轮没有运行新的 spectral 分析；reframe 仍是规划，不是结果。关于 LIIF encoder 全局耦合的解释目前是理论上合理的工作假设，仍需用 frozen/hybrid/control 实验区分。

下一步是否合理：合理。最优先的科研节点应是 Stage B-spectral audit 的冻结协议，而不是继续 patch matching、descriptor、rerank 或训练加速。

防止过度宣称：不能把项目清理写成科学进展；不能把 reframe 写成已证明的 NTK 结论；不能把 LMF 静态 modulation-complexity finding 直接外推成 LIIF scratch fitting dynamics 结论。
