# Daily Memory: 2026-05-09

## Entry 1 - Workspace Documentation And Artifact Reorganization Review

完成范围：

- 按用户要求，暂不继续推进 Stage C 实验，而是对当前工作区文档、代码入口、结果产物和可复现性风险做只读审查。
- 使用 `research-coding-workflow`，将任务判断为 L4 级别：该重构会影响 Stage A/B 封版、Stage C 证据定位和后续论文 claim 边界。
- 启动四个独立 subagent 视角：
  - 文档证据审计：审查 `doc/`、`memory/`、`RESEARCH_PLAN.md` 的 canonical source、过时内容和重构目录。
  - 代码/产物组织审计：审查 `experiments/Phase1_FittingDynamics/`、`tests/`、`results/`、`Data/` 的入口、正式产物与临时风险。
  - 科研审稿视角：审查 Stage A/B/C 证据链、claim 边界和 claim-evidence ledger。
  - 工作流/可复现性审查：审查 dirty worktree、未跟踪文件、ignored 结果产物、误删风险和两阶段安全重构。
- 主流程同步只读盘点：
  - `RESEARCH_PLAN.md`
  - `doc/*.md`
  - `memory/daily/*.md`
  - `experiments/Phase1_FittingDynamics/*.py`
  - `results/` 与 `Data/` 的主要目录结构
  - 当前 git status

运行过的命令与结果：

```bash
sed -n '1,220p' /home/admin/.codex/skills/research-coding-workflow/SKILL.md
sed -n '1,220p' /home/admin/.codex/skills/research-coding-workflow/references/task-orchestration.md
git status --short
find doc -maxdepth 2 -type f | sort
find memory -maxdepth 2 -type f | sort
find experiments/Phase1_FittingDynamics -maxdepth 1 -type f | sort
for f in RESEARCH_PLAN.md doc/*.md memory/daily/*.md; do wc -l "$f"; rg -n "^(#|##|###) " "$f"; done
find results -maxdepth 2 -type d | sort
find Data -maxdepth 3 -printf '%y %p\n' | sort
rg -n "pretrained|LIIF-EQ|SE-INR|训练加速|PCA|claim|失败|Stage C|等变" RESEARCH_PLAN.md doc/*.md
```

主要审查发现：

- Stage A/B 已可封版，但当前文档组织混乱：
  - Stage A canonical 应是 `RESEARCH_PLAN.md`、`doc/StageA_research_questions.md`、`doc/StageA_literature_baseline.md`。
  - Stage B canonical 只应包含 `doc/StageB_observation_log.md` 的 0-6 节；第 7-18 节实际已变成 Stage C pilot / failure audit / gate 记录。
  - `doc/StageAB_review_2026-05-07.md` 前半是 Stage A/B 复盘，后半持续追加 Stage C，且标题编号错乱，不适合作 canonical source。
  - `doc/StageB_observation_protocol.md`、`doc/StageA_remaining_risks.md`、`doc/Phase1_design.md` 均更适合作历史/归档材料。
- `RESEARCH_PLAN.md` 仍有过时内容：
  - Stage B 部分仍列 pretrained LIIF/LIIF-EQ 作为实验阶梯，与当前 scratch reduced LIIF / SIREN 正式证据链冲突。
  - 当前优先事项仍停留在 Stage B smoke 前语境，需要更新为 Stage A/B 封版、Stage C 当前 gate 未过、暂不进入等变性/训练加速。
- Stage C 内容不应继续污染 Stage A/B 文档：
  - baby/woman 正线索、bird/head failure、top-k oracle、response-object audit、LR-cell gate、controlled self-similarity gate 都应集中到 Stage C 文档。
  - Stage C 可以作为后验风险反馈，但不能作为 Stage B 完成证据，也不能反向否定 Stage B 平台。
- 代码入口总体可保留，但需要入口说明：
  - 正式入口：`run.py`、`run_siren.py`、`check_outputs.py`、`analyze_stage_b_controls.py`、Stage C audit/gate scripts、`generate_controlled_self_similarity_images.py`。
  - legacy/谨慎入口：`aggregate_results.py` 默认扫旧 `results/FittingDynamics`，不应作为当前 Stage B 主结论入口；`run_finetune.py` 是 pretrained LIIF/LIIF-EQ 路线，当前应标为 blocked/experimental；`viz_trajectory.py` 偏旧可视化工具。
  - 源码层面未发现新的 launcher/scratch dispatcher；但存在 stale `__pycache__/launch_stage_b_scratch...pyc` 风险信号，后续可清理缓存。
- 结果组织大体合理，但不应移动：
  - `results/FittingDynamics_StageB/` 是正式 Stage B。
  - `results/FittingDynamics_StageC_diagnostics/` 与 `results/FittingDynamics_StageC_controlled_diagnostics/` 是诊断产物。
  - `results/FittingDynamics_StageC_controlled/` 是受控训练结果。
  - `results/FittingDynamics_smoke/` 是 smoke/debug。
  - `results/FittingDynamics/` 是 legacy，不再作为新正式输出目标。
- 可复现性风险：
  - 当前工作区有大量 modified/untracked 文件，且 `results/`、`Data/`、`model/` 被 `.gitignore` 忽略。
  - 直接移动/删除 `results/`、`Data/`、`model/`、`pretrained/` 风险高。
  - 结果 summary 普遍缺少 git commit、完整命令、环境、脚本版本，主要靠 `doc/` 和 `memory/` 追溯，而这些文档本身仍未固化。

证据支持的重构判断：

- 第一阶段不应直接搬目录或删除结果，应先建立索引、claim-evidence ledger 和 canonical 文档结构。
- Stage A/B 可封版为：
  - Stage A：研究问题、假设、非目标、负对照和第一轮文献地图足以支撑 pilot，但不是论文级 related work。
  - Stage B：scratch fitting-dynamics 平台完成，只证明记录、检查、聚合、控制分析可靠；PCA/PC1 只能作为诊断，不支撑局部几何、等变性或训练加速 claim。
- Stage C 当前应单独定位为 pilot/failure audit：
  - 有 baby/woman 局部线索，但 bird/head、response-object audit、LR-cell gate、controlled gate 均说明当前操作化不足。
  - 不能把 Stage C 正例写成跨图像规律，也不能把 Stage C failure 写成研究方向整体失败。

建议的重构路线：

1. 先新增项目索引/ledger，不搬原始结果：
   - `doc/README.md`：说明 canonical source、archive、memory、results 语义。
   - `doc/claims_ledger.md`：列出 A/B/C/X claim、允许表述、证据文件、反证、状态、caveat、next gate。
   - `results/README.md` 或 `doc/results_manifest.md`：只索引结果目录，不移动结果。
2. 再重构文档目录：
   - `doc/stage_a/research_questions.md`
   - `doc/stage_a/literature_baseline.md`
   - `doc/stage_b/observation_record.md`
   - `doc/stage_b/controls_summary.md`
   - `doc/stage_c/decision_gate_2026-05-08.md`
   - `doc/stage_c/failure_audit.md`
   - `doc/stage_c/controlled_self_similarity.md`
   - `doc/archive/` 保存 `Phase1_design.md`、旧 `StageAB_review_2026-05-07.md`、旧协议和已过时风险记录。
3. 更新 `RESEARCH_PLAN.md`：
   - 标明 Stage A/B 已完成的边界。
   - 移除或降级 Stage B 中 pretrained LIIF/LIIF-EQ 作为当前阶段前置的表述。
   - 明确 Stage C 当前 gate 未过，暂不进入等变性、pretrained、update prediction 或训练加速。
4. 增加实验入口说明：
   - 在 `experiments/Phase1_FittingDynamics/README.md` 或同等文档中标注正式入口、legacy 入口、blocked 入口。
   - 明确正式训练必须显式 `--save_dir`，避免默认写入旧 `results/FittingDynamics/...`。
5. 最后才考虑轻量清理：
   - 可清理 `__pycache__`。
   - 是否接受 `launch_all.py` 删除需与用户确认。
   - 不移动 `results/`、`Data/`、`model/`、`pretrained/`，除非先生成路径映射并得到用户确认。

未解决风险与下一步：

- 本轮只提出方案，没有执行文档迁移或代码清理。
- 需要用户确认是否按“两阶段安全重构”执行：
  - 阶段 1：只新增索引、ledger、README，保留原文件。
  - 阶段 2：在索引确认后再移动/归档旧文档并更新引用。
- 需要确认 `launch_all.py` 删除是正式接受还是需要历史归档。
- 需要确认 `memory/daily/` 是否纳入 git 管理；按项目规则它是项目记忆，但当前仍显示为未跟踪。

## 专家评审

可靠：四个独立视角结论一致，均认为当前最大问题不是代码逻辑，而是 canonical source、claim 边界和可追溯性混乱；Stage A/B 可封版，但 Stage C 不应继续混入 A/B 文档。对文档、入口、结果目录和 git 状态均做了本地只读核查。

不足：本轮没有实际重构文档，也没有运行测试；方案尚未验证链接更新、路径引用和 markdown 结构完整性。

下一步是否合理：最合理的是先做阶段 1：新增索引、claim-evidence ledger 和入口/结果说明；不要马上移动大目录或删除旧文档。阶段 1 完成并经用户确认后，再进行最小文档迁移。

防止过度宣称：重构文档时必须把 Stage B 封为“平台可靠”，不能写成科学机制成立；Stage C 正例只能写成局部线索，failure audit 只能说明当前操作化不足，不能证明研究方向整体失败。

## Entry 2 - Paper-Level Reframing Of Workspace Reorganization

完成范围：

- 根据用户反馈，重新审视前一版重构方案的不足：它主要针对现有文件收纳和当前实验路径，仍偏“整理现状”，没有充分建立论文级项目所需的证据系统、思考路径和未来分支承载能力。
- 重新读取 `research-coding-workflow` 的 paper-writing 与 experiment-workflow 参考，明确论文级项目需要：
  - claim-evidence ledger；
  - hypothesis-first experiment card；
  - raw evidence / derived analysis / interpretation / decision 的分层；
  - paper claim 与代码、结果、文献、失败案例的一一对应；
  - 可承载后续 Stage C 重定义、等变性分支或降级路线的结构。
- 主流程补充检查：
  - `paper/` 当前已有 manuscript skeleton；
  - `src/` 与 `experiments/` 仍混有正式入口、legacy 入口和 blocked pretrained 入口；
  - `.gitignore` 忽略 `results/`、`Data/`、`model/`，因此必须依赖 manifest/registry 保存证据路径语义。

证据支持的判断：

- 当前根本问题不是“文档太多”，而是缺少项目级知识架构：
  - 日志型文档被当作 canonical source；
  - Stage C 后验诊断污染 Stage A/B 封版文档；
  - 实验结果有文件路径，但缺少统一 run registry、experiment card 和 claim linkage；
  - 失败案例被记录了，但尚未作为 first-class negative evidence 纳入 claim ledger；
  - 论文骨架存在，但 paper claim 与本地证据链尚未绑定。
- 论文级重构应从“证据系统”开始，而不是从“移动文件”开始。

修正后的重构原则：

1. 区分四层材料：
   - `canonical`：当前项目事实与允许引用的结论；
   - `evidence`：实验卡、run manifest、analysis manifest、结果索引、失败案例；
   - `decision`：stage gate、路线选择、降级/继续条件；
   - `archive`：历史草稿、过时协议、append-only daily memory。
2. 每条结论必须能追到：
   - 对应 hypothesis；
   - 实验/分析协议；
   - raw result 目录或 diagnostic JSON；
   - 控制实验与反证；
   - 允许论文表述与禁止表述。
3. 未来分支必须预留位置：
   - 若 Stage C 继续挽救：model-internal unit / geometry-content dissociation gate；
   - 若 Stage C 降级：failure-audit / diagnostic framework；
   - 若未来进入 Stage D：等变性、update prediction、training reduction 必须先通过明确前置 gate。

下一步建议：

- 不直接移动旧文档。
- 先实现 paper-level skeleton：
  - `doc/project_index.md`
  - `doc/claims_ledger.md`
  - `doc/experiment_registry.md`
  - `doc/decision_log/`
  - `doc/evidence/`
  - `doc/archive/README.md`
- 再从旧文档抽取 Stage A/B/C canonical closeout，而不是简单复制/移动。

## 专家评审

可靠：用户指出的问题成立；前一版方案偏目录整理，未充分解决论文级项目的证据链、思考链和未来分支管理。新的判断与 paper-writing / experiment-workflow 要求一致。

不足：本轮仍未实际建立新 skeleton，也未验证后续迁移成本；需要下一轮把抽象原则落成具体文件和最小可执行模板。

下一步是否合理：应先创建索引、ledger、experiment registry 和 decision log 模板，再整理 Stage A/B canonical。这样能避免旧文档继续膨胀，也避免把当前失败或正例写成不合格 claim。

防止过度宣称：重构方案本身不能把当前研究包装成论文成果；它只能提高可追溯性。论文级结构必须同样保存负结果、失败 gate 和禁止 claim。

## Entry 3 - Paper-Level Evidence Skeleton Implementation

完成范围：

- 按用户确认，执行论文级项目骨架第一阶段：新增索引、研究合同、claim ledger、evidence registry、decision log 和 stage closeout。
- 未移动旧文档，未移动 `results/`、`Data/`、`model/`、`pretrained/`，未删除任何实验结果。
- 新增实验入口说明，明确正式入口、legacy 入口和 blocked 入口。
- 小范围更新 `RESEARCH_PLAN.md`，使其指向新证据入口，并修正 Stage B 当前状态与优先事项中过时表述。

新增文件：

- [`doc/project_index.md`](../../doc/project_index.md)
- [`doc/research_contract.md`](../../doc/research_contract.md)
- [`doc/claims_ledger.md`](../../doc/claims_ledger.md)
- [`doc/evidence/experiment_registry.md`](../../doc/evidence/experiment_registry.md)
- [`doc/evidence/run_manifest_stage_b.md`](../../doc/evidence/run_manifest_stage_b.md)
- [`doc/evidence/analysis_manifest_stage_c.md`](../../doc/evidence/analysis_manifest_stage_c.md)
- [`doc/evidence/result_artifact_policy.md`](../../doc/evidence/result_artifact_policy.md)
- [`doc/decisions/README.md`](../../doc/decisions/README.md)
- [`doc/decisions/2026-05-07_stage_ab_freeze.md`](../../doc/decisions/2026-05-07_stage_ab_freeze.md)
- [`doc/decisions/2026-05-08_stage_c_operationalization_failure.md`](../../doc/decisions/2026-05-08_stage_c_operationalization_failure.md)
- [`doc/stages/stage_a/closeout.md`](../../doc/stages/stage_a/closeout.md)
- [`doc/stages/stage_a/research_questions.md`](../../doc/stages/stage_a/research_questions.md)
- [`doc/stages/stage_a/literature_map.md`](../../doc/stages/stage_a/literature_map.md)
- [`doc/stages/stage_b/closeout.md`](../../doc/stages/stage_b/closeout.md)
- [`doc/stages/stage_b/observation_record.md`](../../doc/stages/stage_b/observation_record.md)
- [`doc/stages/stage_b/controls_summary.md`](../../doc/stages/stage_b/controls_summary.md)
- [`doc/stages/stage_c/current_status.md`](../../doc/stages/stage_c/current_status.md)
- [`doc/stages/stage_c/failure_audit.md`](../../doc/stages/stage_c/failure_audit.md)
- [`doc/stages/stage_c/controlled_self_similarity.md`](../../doc/stages/stage_c/controlled_self_similarity.md)
- [`doc/stages/stage_c/future_gates.md`](../../doc/stages/stage_c/future_gates.md)
- [`doc/archive/README.md`](../../doc/archive/README.md)
- [`experiments/Phase1_FittingDynamics/README.md`](../../experiments/Phase1_FittingDynamics/README.md)

修改文件：

- [`RESEARCH_PLAN.md`](../../RESEARCH_PLAN.md)
  - 增加 2026-05-09 当前事实源说明，指向 `doc/project_index.md`、`doc/claims_ledger.md`、`doc/evidence/experiment_registry.md`、`doc/stages/`、`doc/decisions/`。
  - 将 Stage B 当前状态标明为 scratch reduced LIIF / SIREN 平台验证已封版。
  - 将 pretrained LIIF/LIIF-EQ Stage B 微调条款降级为历史计划，当前不执行。
  - 将当前优先事项改为：使用新证据入口、维持 A/B 封版、Stage C 只允许明确 gate、暂不进入等变性/训练加速。

运行过的检查：

```bash
python - <<'PY'
from pathlib import Path
import re, sys
files=[Path('RESEARCH_PLAN.md')] + sorted(Path('doc').rglob('*.md')) + [Path('experiments/Phase1_FittingDynamics/README.md')]
pat=re.compile(r'\[[^\]]+\]\(([^)]+)\)')
broken=[]
for f in files:
    if not f.exists():
        continue
    text=f.read_text(errors='ignore')
    for m in pat.finditer(text):
        target=m.group(1).split('#',1)[0]
        if not target or '://' in target or target.startswith('mailto:'):
            continue
        p=(f.parent/target).resolve()
        if not p.exists():
            broken.append((str(f), target))
if broken:
    print('BROKEN LINKS')
    for f,t in broken:
        print(f, '->', t)
    sys.exit(1)
print(f'checked {len(files)} markdown files; no broken local links')
PY
```

结果：`checked 32 markdown files; no broken local links`。

证据支持的结论：

- 现在项目已有一个比旧 `StageAB_review` 更适合作为论文级入口的骨架：
  - `project_index.md` 管入口；
  - `research_contract.md` 管研究边界；
  - `claims_ledger.md` 管允许/禁止表述；
  - `evidence/` 管实验与结果索引；
  - `decisions/` 管研究路线决策；
  - `stages/` 管 A/B/C 当前 closeout 与状态。
- 旧文档没有被删除，仍可审计；但新读者应从 `doc/project_index.md` 开始，而不是从 `StageAB_review_2026-05-07.md` 开始。
- 当前 skeleton 已明确保留负结果和失败 gate，不把 Stage C 包装成正结论。

未解决风险与下一步：

- 新 closeout 文件目前是第一版抽取摘要，不是完整迁移；后续需要把关键数值和命令逐步从旧文档和 JSON 中补齐。
- 旧 `doc/StageB_observation_log.md`、`doc/StageAB_review_2026-05-07.md` 等还未归档，仍可能被误读；下一步应在用户确认后给旧文件顶部加 superseded notice 或移动到 `doc/archive/`。
- `claims_ledger.md` 仍需后续扩展到更完整的 claim-id 体系，特别是论文 manuscript 各段落可能使用的 claim。
- `results/` 仍未有机器可读 manifest；当前 `doc/evidence/*.md` 是人工索引。

## 专家评审

可靠：本轮按“先建立证据系统、暂不搬旧文件”的安全策略执行；新增文档将 claim、evidence、decision、stage status 分开，避免继续用混杂日志当事实源；链接检查通过。

不足：这只是第一阶段 skeleton，尚未完成旧文档归档、结果 manifest 机器化、paper 段落 claim 对齐，也未运行代码测试，因为本轮只改文档和 README。

下一步是否合理：下一步应先补 `claims_ledger.md` 和 `evidence/` 的细节，再处理旧文档顶部 superseded notice / archive；不建议马上删除或移动结果目录。

防止过度宣称：新结构必须继续把 Stage B 限定为平台可靠，把 Stage C 限定为 pilot/failure audit；controlled positive 和 baby/woman 线索都不能升级为 geometry mechanism claim。

## Entry 4 - Final Workspace Architecture And Evidence-Chain Review

完成范围：

- 按用户要求，对当前项目架构、具体文件内容、内容构成方式和组织形式做最后一轮只读核查。
- 使用 `research-coding-workflow`，将任务判定为 L3/L4 边界任务：本轮不改科学结论，但审查结果会影响后续论文级证据链、文档入口和实验继续条件。
- 启动两个只读独立 subagent：
  - 文档架构与入口视角：审查新读者是否能从当前文档找到可信结论、阶段状态、下一步和历史材料。
  - 证据链、claim 与 paper 风险视角：审查 `claims_ledger`、`research_contract`、`evidence`、`paper/` 和旧文档是否存在过度宣称或误用风险。
- 第三个代码入口、实验产物与可复现性卫生视角因 subagent 线程上限未能启动，由主流程显式完成，未伪装成独立复核。

本轮检查过的重点文件和目录：

- `doc/project_index.md`
- `doc/research_contract.md`
- `doc/claims_ledger.md`
- `doc/evidence/*`
- `doc/decisions/*`
- `doc/stages/*`
- `doc/archive/README.md`
- `doc/Refactor_plan.md`
- `doc/Phase1_design.md`
- `doc/StageAB_review_2026-05-07.md`
- `doc/StageB_observation_log.md`
- `RESEARCH_PLAN.md`
- `experiments/Phase1_FittingDynamics/README.md`
- `experiments/Phase1_FittingDynamics/*.py`
- `paper/main.tex`
- `paper/sections/*.tex`
- `paper/refs/main.bib`
- `results/`、`Data/`、`model/`、`pretrained/` 的目录级状态

运行过的命令与结果：

```bash
git status --short
find doc -maxdepth 3 -type f | sort
find experiments/Phase1_FittingDynamics -maxdepth 2 -type f | sort
find paper -maxdepth 3 -type f | sort
sed -n '1,220p' doc/project_index.md
sed -n '1,220p' doc/claims_ledger.md
sed -n '1,220p' doc/research_contract.md
sed -n '1,260p' doc/Refactor_plan.md
sed -n '1,260p' experiments/Phase1_FittingDynamics/README.md
sed -n '1,220p' RESEARCH_PLAN.md
sed -n '220,520p' RESEARCH_PLAN.md
sed -n '1,160p' paper/main.tex
sed -n '1,120p' paper/sections/00_abstract.tex
sed -n '1,160p' paper/sections/01_introduction.tex
sed -n '1,220p' doc/evidence/experiment_registry.md
sed -n '1,220p' doc/evidence/run_manifest_stage_b.md
sed -n '1,220p' doc/evidence/analysis_manifest_stage_c.md
find . -path './.venv' -prune -o -type f \( -name '*launch*.py' -o -name '*launcher*' -o -name '*.sh' -o -name '*nohup*' -o -name '*batch*' \) -print | sort
find . -path './.venv' -prune -o \( -type d -name '__pycache__' -o -type f -name '*.pyc' \) -print | sort
find results -maxdepth 2 -type d | sort
du -sh Data results model pretrained
rg -n "This paper provides the answer|30%|proves|proof|equivariance improves|training acceleration|low-dimensional|LIIF-EQ|pretrained" doc paper RESEARCH_PLAN.md experiments/Phase1_FittingDynamics/README.md
```

链接检查结果：

```bash
# canonical docs / readme scope
checked 138 canonical doc/readme links; no broken local links

# all markdown scope
checked 232 local markdown links; missing 11
```

全仓库 Markdown 链接缺失全部来自 `memory/daily/2026-05-07.md` 的旧相对链接和已删除 `doc/Project_workflow.md` 链接。由于记忆文件是 append-only 历史记录，本轮没有静默改写旧记录；这应作为历史可读性风险后续处理。

主要审查结论：

- 新证据骨架方向正确：
  - `doc/project_index.md` 能作为当前入口；
  - `doc/research_contract.md` 能固定研究边界、阶段边界和 forbidden claims；
  - `doc/claims_ledger.md` 已能阻止 Stage B/C 被过度解释；
  - `doc/evidence/`、`doc/decisions/`、`doc/stages/` 初步形成 raw/analysis/decision/stage closeout 的分层。
- 最大 P0 风险是旧 `paper/`：
  - `paper/sections/00_abstract.tex` 仍写 “This paper provides the answer”；
  - 旧 paper 仍宣称 low-dimensional orbit、Lie algebra representation、COIN++ 解释和 `>=30%` tangent-based fast adaptation；
  - 这些 claim 与当前 `research_contract` 和 `claims_ledger` 冲突；
  - 旧 paper 还可能不可编译，因 `paper/figures/` 只有 `.gitkeep`，但正文包含 `\input{figures/...}`，且存在未定义环境/命令和 `\todo` 残留。
- P0/P1 风险还包括旧根目录文档：
  - `doc/Phase1_design.md` 仍以参数轨迹低维、PCA、LIIF-EQ 扩展为核心旧路线；
  - `doc/StageAB_review_2026-05-07.md` 和 `doc/StageB_observation_log.md` 包含大量历史追加和 Stage C 混合内容，不适合作 canonical source；
  - `doc/StageB_observation_protocol.md`、`doc/StageA_remaining_risks.md` 已过时但文件自身缺少顶部 superseded banner。
- P1 风险是证据索引仍偏人工摘要：
  - `doc/evidence/experiment_registry.md` 已有实验类别索引，但缺少每个关键实验的 exact command、参数、primary metric key、JSON path、日志路径、commit/diff 状态和 caveat；
  - `doc/claims_ledger.md` 还缺少 `PAPER-OLD-*` 类 forbidden claims，用于显式阻止旧 group-orbit / tangent-speedup 叙事回流。
- P1 风险是版本边界不稳定：
  - `git status --short` 显示大量 modified/untracked 文件，包括新文档、分析脚本、测试和 memory；
  - `results/`、`Data/`、`model/`、`pretrained/` 均被 `.gitignore` 忽略，且目录体量分别约为 `276M`、`5.7G`、`901M`、`50M`；
  - 在继续实验前应形成审查性 commit 或至少生成变更清单，否则“当前证据源”容易随工作区漂移。
- 代码入口组织总体合理：
  - `experiments/Phase1_FittingDynamics/README.md` 对 official training、official validation/analysis、legacy/blocked entrypoints 的分类是清楚的；
  - 项目内未发现新的正式源码级临时 launcher；
  - 但存在项目内 `__pycache__` 与 stale `launch_stage_b_scratch.cpython-312.pyc`，属于可读性/卫生问题；本轮未清理。
- 数据与结果组织基本符合当前分层：
  - `results/FittingDynamics_StageB/` 是正式 Stage B；
  - `results/FittingDynamics_StageC_diagnostics/`、`results/FittingDynamics_StageC_controlled_diagnostics/` 是 Stage C 诊断；
  - `results/FittingDynamics_StageC_controlled/` 是受控训练结果；
  - `results/FittingDynamics_smoke/` 是 smoke/debug；
  - `results/FittingDynamics/` 是 legacy，不应作为新正式输出。

证据支持的判断：

- 当前项目不是“基础全盘失败”，而是已经从混乱旧叙事过渡到较合格的证据骨架；Stage A/B 的当前组织能支撑 pilot closeout 和平台封版。
- 当前仍不是论文级可交接状态，因为旧 `paper/` 和旧根目录文档会绕过新 ledger，把已经否定或未验证的强 claim 带回来。
- Stage C 的 current status 写法是保守合格的：它承认 baby/woman 局部线索，也承认 bird/head failure 和当前操作化失败；没有把失败包装成主贡献。

建议的下一步修正顺序：

1. 做入口硬化，不改科学内容：
   - 新增极短 `doc/README.md`，只指向 `doc/project_index.md`、`claims_ledger`、`evidence`、`decisions`、`archive`。
   - 新增 `paper/README.md`，声明旧 `paper/` 是 historical non-canonical draft，不是当前论文证据源。
   - 在 `paper/main.tex` 顶部加注释说明禁止把旧稿 claim 作为当前结论。
2. 给旧根目录文档加统一顶部 superseded banner：
   - `doc/Phase1_design.md`
   - `doc/StageB_observation_protocol.md`
   - `doc/StageA_remaining_risks.md`
   - `doc/StageAB_review_2026-05-07.md`
   - 视情况也给 `doc/StageB_observation_log.md` 标明只有前半 Stage B 记录可作为历史证据，当前 canonical 以 `doc/stages/` 和 `doc/evidence/` 为准。
3. 扩展 `doc/claims_ledger.md`：
   - 增加 `PAPER-OLD-*` forbidden claims，包括 group orbit、Lie homomorphism、COIN++ low-dimensional explanation、tangent fast adaptation `>=30%`。
4. 补最小可复现实验卡：
   - 先覆盖 Stage B check/control；
   - 再覆盖 Stage C failure audit、response-object audit、patch-unit gate、controlled self-similarity。
5. 在继续实验前形成变更清单或审查性 commit，并考虑清理项目内 `__pycache__`。

未解决风险：

- 本轮没有修复上述问题，只完成只读审查和记忆追加。
- 没有运行测试，因为本轮没有代码行为修改；若后续做入口硬化和文档链接修改，应至少重跑 canonical markdown link check。
- `memory/daily/2026-05-07.md` 的历史链接缺失仍存在；由于记忆文件 append-only，需决定是保留历史原貌并追加勘误，还是谨慎修复旧链接。
- `paper/refs/main.bib` 仍有未核实条目，旧 paper 已引用；后续应做 bib verification 表。

## 专家评审

可靠：两个独立 subagent 与主流程结论一致，均认为新证据骨架合格、最大风险是旧 `paper/` 和旧文档的误导性入口；canonical 文档链接检查通过，说明新骨架内部连接目前稳定。

不足：第三个代码/产物卫生视角没有独立 subagent，只由主流程审查；本轮也没有做修复、测试或提交，因此仍停留在最终审查结论而非整改完成状态。

下一步是否合理：应先做入口硬化和旧文档 superseded banner，这是低风险、高收益的整理；不应马上重写论文正文、移动大目录、删除结果或继续 Stage C 实验。

防止过度宣称：后续任何论文写作前必须先处理旧 `paper/` 的强 claim；Stage B 仍只能写平台可靠，Stage C 仍只能写 pilot / failure audit，不能把 baby/woman 线索、controlled positive 或旧 group-orbit 草稿升级成机制结论。

## Entry 5 - Markdown Redundancy And Paper-Facing Organization Review

完成范围：

- 按用户要求，从专业代码工程师和论文评审视角，重新审查当前项目组织是否杂乱、已有 Markdown 内容是否都有保留必要，重点判断论文冗余度。
- 本轮未改文档结构，只做只读统计和分层判断。
- 当前请求未显式要求新 subagent，本轮在主流程内模拟两个独立视角：
  - 工程维护视角：哪些文档会增加维护负担、入口歧义和版本漂移。
  - 论文评审视角：哪些文档会让论文叙事冗余、claim 反复回流或证据边界不清。

运行过的命令与结果：

```bash
find . -path './.venv' -prune -o -path './.git' -prune -o -name '*.md' -type f -print | sort
find doc -name '*.md' -type f -print0 | xargs -0 wc -l | sort -n
find . -maxdepth 2 -name '*.md' -type f -print0 | xargs -0 wc -l | sort -n
find memory/daily -name '*.md' -type f -print0 | xargs -0 wc -l | sort -n
for f in doc/*.md RESEARCH_PLAN.md experiments/Phase1_FittingDynamics/README.md; do rg -n "^(#|##|###) " "$f"; done
for f in doc/stages/*/*.md doc/evidence/*.md doc/decisions/*.md doc/archive/README.md; do rg -n "^(#|##|###) " "$f"; done
find .claude -maxdepth 3 -type f -name '*.md' -print0 | xargs -0 wc -l | sort -n
for f in .claude/rules/*.md; do sed -n '1,120p' "$f"; done
git status --short -- .claude doc RESEARCH_PLAN.md paper memory
```

规模事实：

- 当前非 `.venv`/非 `.git` Markdown 至少包括：
  - `doc/` 下 30 个 Markdown，共约 4583 行；
  - `memory/daily/` 下 3 个 Markdown，共约 2148 行；
  - `.claude/rules/` 下 5 个 Markdown，共约 337 行；
  - 根目录 `RESEARCH_PLAN.md` 约 317 行；
  - `Data/README.md` 约 160 行；
  - `experiments/Phase1_FittingDynamics/README.md` 约 35 行。
- `doc/` 中体量最大的文档是旧日志/旧 gate：
  - `doc/StageB_observation_log.md` 约 1147 行；
  - `doc/StageAB_review_2026-05-07.md` 约 683 行；
  - `doc/StageC_decision_gate_2026-05-08.md` 约 534 行；
  - `doc/StageA_literature_baseline.md` 约 333 行；
  - `doc/Phase1_design.md` 约 267 行；
  - `doc/Refactor_plan.md` 约 255 行。

审查结论：

- 当前 active 文档面确实杂乱。问题不是“Markdown 多”本身，而是多类文档混在同一可见层级：
  - 当前事实源；
  - 历史计划；
  - 每日/追加日志；
  - decision gate 执行记录；
  - evidence manifest；
  - 工程重构计划；
  - 通用/半通用协作规则；
  - 旧论文草稿。
- 这些内容并非都应删除；但多数不应保留为 active 文档。
- 从论文冗余度看，最危险的是同一问题被多份文档反复表述：
  - Stage A 问题在 `RESEARCH_PLAN.md`、`doc/StageA_research_questions.md`、`doc/stages/stage_a/research_questions.md`、`doc/research_contract.md` 中重复；
  - Stage B 记录在 `doc/StageB_observation_log.md`、`doc/stages/stage_b/*`、`doc/evidence/run_manifest_stage_b.md` 中重复；
  - Stage C failure/gate 在 `doc/StageB_observation_log.md` 后半、`doc/StageAB_review_2026-05-07.md` 后半、`doc/StageC_decision_gate_2026-05-08.md`、`doc/stages/stage_c/*`、`doc/evidence/analysis_manifest_stage_c.md` 中重复；
  - 旧 paper 叙事和当前 `claims_ledger` 明确冲突。
- `.claude/rules/*.md` 是额外隐性问题：
  - `coding.md`、`testing.md`、`security.md` 多数是跨项目通用规则，不应继续放在项目目录；
  - `latex.md` 绑定旧 manuscript 组织和旧符号系统，可能让旧 group-orbit 论文叙事继续影响当前项目；
  - `architecture.md` 有一部分项目特定事实，可抽取到工程文档，但不应作为隐藏规则长期影响项目。

保留必要性判断：

- 必须保留为 active canonical 的最小集合应很小：
  - `doc/project_index.md`
  - `doc/research_contract.md`
  - `doc/claims_ledger.md`
  - `doc/evidence/experiment_registry.md`
  - `doc/evidence/run_manifest_stage_b.md`
  - `doc/evidence/analysis_manifest_stage_c.md`
  - `doc/decisions/README.md`
  - 现有 decision records
  - 一个精简后的 Stage A/B/C status 或 closeout 文件组
  - `experiments/Phase1_FittingDynamics/README.md`
  - 一个更新后的工程边界文档，可能由 `doc/Refactor_plan.md` 精简得到。
- 应转为 archive/historical 的内容：
  - `doc/Phase1_design.md`
  - `doc/StageAB_review_2026-05-07.md`
  - `doc/StageB_observation_protocol.md`
  - `doc/StageB_observation_log.md`
  - `doc/StageC_decision_gate_2026-05-08.md`
  - `doc/StageA_remaining_risks.md`
  - 旧 `paper/` 草稿，至少必须标为 historical non-canonical。
- 应合并或降级的内容：
  - `doc/stages/stage_a/research_questions.md` 可并入 `doc/research_contract.md` 或作为 Stage A closeout 摘要；
  - `doc/stages/stage_a/literature_map.md` 与 `doc/StageA_literature_baseline.md` 应分工：一个是当前简表，一个是历史详细表，不能两者都 active；
  - `doc/stages/stage_c/current_status.md`、`failure_audit.md`、`controlled_self_similarity.md`、`future_gates.md` 可以合并为一个 Stage C status，详细 artifact 留在 `doc/evidence/analysis_manifest_stage_c.md`；
  - `doc/evidence/result_artifact_policy.md` 可并入 `experiment_registry.md` 或保留为短 policy，但不应扩展成第二套 registry。
- 不应作为项目 Markdown 保留的内容：
  - 通用 `.claude/rules/coding.md`、`testing.md`、`security.md` 应迁移到全局规则或删除；
  - `.claude/rules/latex.md` 若保留，必须重写为当前论文骨架规则，不再绑定旧 group-orbit 草稿；
  - `.pytest_cache/README.md` 不是项目知识资产，不计入项目文档。

建议的去冗余目标：

- 将 active `doc/` 文档从 30 个压缩到约 10-14 个。
- 根目录保留一个入口即可；`RESEARCH_PLAN.md` 应降级为短 roadmap，或把当前事实源完全交给 `doc/project_index.md`。
- `doc/archive/` 应真正承载历史材料，而不是只写候选列表。
- 每份 active 文档只回答一个问题：
  - `project_index`：从哪里开始读；
  - `research_contract`：研究问题和禁止 claim；
  - `claims_ledger`：什么能写、什么不能写；
  - `evidence`：证据在哪里、如何复现；
  - `decisions`：为什么停止/继续；
  - `stage status`：当前阶段状态；
  - `engineering contract`：代码入口和可维护性边界。

证据支持的判断：

- 当前内容不是都“没有必要”；旧日志和旧 gate 对审计有价值。
- 但从论文写作和项目交接看，旧日志不应出现在 active 文档面，否则会显著增加误读和过度宣称概率。
- 最合理的下一步不是继续增加文档，而是做文档减法：压缩 active surface、移动历史材料、给旧 paper 和旧文档加 non-canonical 标识。

未解决风险：

- 本轮没有执行归档或合并。
- 文档去冗余如果做得过猛，可能丢失实验细节；因此应先移动到 archive，不直接删除。
- 需要先确定是否允许移动 `.claude/rules` 或只在本项目中标记废弃，因为这些可能是其他工具的配置入口。

## 专家评审

可靠：文档数量、行数和标题结构均已本地统计；结论由实际文件分布支持。当前 active 文档面确实过宽，论文叙事存在多源重复和旧 claim 回流风险。

不足：本轮只做判断，没有实际合并、归档或链接修复；没有重新跑链接检查，因为未改文档结构。

下一步是否合理：应执行一次“文档减法”重构，优先 archive 旧日志/旧协议/旧 gate，硬化 `paper/` historical 标识，再精简 Stage A/B/C active status。不要继续新增平行总结文档。

防止过度宣称：去冗余不是为了让项目看起来更成功，而是让当前不充分、失败和 blocked 的证据更难被忽略。尤其不能让旧 `paper/` 或旧 `latex.md` 继续暗示 group-orbit、Lie homomorphism 或训练加速已经成立。

## Entry 6 - Documentation Reduction Refactor

完成范围：

- 按用户确认，执行文档减法和入口硬化。
- 本轮只改文档组织与说明，不改实验结果、不改训练/分析代码行为、不删除数据或结果目录。
- 将旧计划、旧日志、旧 gate 和项目内通用工具规则移入 archive，降低 active 文档面。
- 最后按用户要求做了复核：检查 active 文档面、archive 内容、Markdown 链接、旧强 claim 入口、git 状态和结果目录是否被误动。

主要改动：

- 新增当前文档入口：
  - `doc/README.md`
  - `paper/README.md`
  - `doc/archive/historical_docs/README.md`
  - `doc/archive/stage_summaries_2026-05-09/README.md`
- 历史文档移动到 `doc/archive/historical_docs/`：
  - `Phase1_design.md`
  - `StageA_research_questions.md`
  - `StageA_literature_baseline.md`
  - `StageA_remaining_risks.md`
  - `StageAB_review_2026-05-07.md`
  - `StageB_observation_log.md`
  - `StageB_observation_protocol.md`
  - `StageC_decision_gate_2026-05-08.md`
- 项目内通用/半通用 `.claude/rules/*.md` 移动到 `doc/archive/claude_rules_2026-05-09/`，不再作为当前项目事实源。
- 阶段小摘要移动到 `doc/archive/stage_summaries_2026-05-09/`，active `doc/stages/` 压缩为：
  - `doc/stages/stage_a/closeout.md`
  - `doc/stages/stage_b/closeout.md`
  - `doc/stages/stage_c/current_status.md`
- `doc/Refactor_plan.md` 从 255 行长历史重构计划改为 121 行当前工程边界。
- `paper/main.tex` 顶部增加 historical non-canonical 注释；`paper/README.md` 明确旧 manuscript 不代表当前 claim。
- `doc/claims_ledger.md` 增加 `PAPER-OLD-*` blocked claims，防止旧 group-orbit / Lie homomorphism / COIN++ explanation / `>=30%` speedup 叙事回流。
- `RESEARCH_PLAN.md` 更新为短 roadmap 定位，明确当前事实源以 `doc/project_index.md`、`claims_ledger`、`evidence`、`stages`、`decisions` 为准。

复核命令与结果：

```bash
find doc -path 'doc/archive' -prune -o -type f -name '*.md' -print | sort
find doc -path 'doc/archive' -prune -o -name '*.md' -type f -print0 | xargs -0 wc -l | sort -n
```

结果：active `doc/` 已压缩为 15 个 Markdown，共 771 行；归档前 `doc/` 为 30 个 Markdown，共约 4583 行。

```bash
python <markdown local link checker>
```

结果：`checked 166 markdown links; broken 0`，范围包括 `RESEARCH_PLAN.md`、`doc/**/*.md`、`experiments/Phase1_FittingDynamics/README.md`、`paper/README.md`。

```bash
python <all markdown local link checker>
```

结果：`checked 260 all markdown links; broken 50`。断链全部来自 `memory/daily/*.md` 历史记录中指向旧 doc 根路径或已删除 `Project_workflow.md` 的链接。由于 daily memory 是 append-only 历史记录，本轮未改写旧记忆；同时也没有在 `doc/` 根层重建旧文件，以免抵消文档减法。

```bash
rg -n "This paper provides the answer|>=30%|We prove|discovers a stable|Equivariance improves fitting dynamics|Current method reduces|Stage B proves geometry|PCA proves|low-dimensional orbital" doc RESEARCH_PLAN.md paper/README.md paper/main.tex experiments/Phase1_FittingDynamics/README.md
```

结果：命中均处在 `claims_ledger.md` 的 forbidden/blocked wording、`paper/README.md` / `paper/main.tex` 的 historical warning，或 archive 内历史文件；active 正向叙事未发现旧强 claim 回流。

```bash
find results Data model pretrained -maxdepth 1 -type d 2>/dev/null | sort
```

结果：`results/`、`Data/`、`model/`、`pretrained/` 仍在原位；本轮未移动或删除实验产物。

证据支持的结论：

- 本轮完成了实质文档减法：active `doc/` 从 30 个文件/约 4583 行降到 15 个文件/771 行。
- 旧日志、旧协议、旧 gate 和旧规则没有删除，均可从 `doc/archive/README.md` 追溯。
- 当前论文级入口更清楚：`doc/README.md` -> `doc/project_index.md` -> `research_contract` / `claims_ledger` / `evidence` / `decisions` / `stages`。
- 旧 paper 已被明确标为 historical non-canonical，不能再被误读为当前论文草稿。

未解决风险：

- `memory/daily/*.md` 的历史链接仍有断链；这是 append-only 记忆与归档重构之间的残留问题。后续可选择追加一个 memory link map，但不建议在 `doc/` 根层重建旧文件。
- `.claude/rules/*.md` 被移动后，若 Claude Code 仍依赖这些项目本地规则，需要将真正跨项目规则迁移到全局配置，而不是恢复到项目目录。
- `paper/refs/main.bib` 仍有未核实条目，未在本轮处理。
- git 工作区仍有大量前序 modified/untracked 文件；本轮没有提交，也没有触碰无关代码改动。

## 专家评审

可靠：本轮修改符合“保留证据、减少 active 面”的目标；没有删除实验产物；非 memory 范围内 Markdown 链接全部通过；旧强 claim 已被限制在 blocked/forbidden 或 archive/historical warning 语境。

不足：这仍是文档组织层面的整改，不是科学问题本身的解决；archive 内历史材料仍很多，后续读者若绕过 `doc/README.md` 仍可能看到旧内容，但路径和 README 已明确标注其非 canonical 地位。

下一步是否合理：下一步不应继续新增总结文档；如果继续整理，应补最小 experiment cards 或做 bib verification。科研推进前，应先确认这批文档重构进入稳定版本边界。

防止过度宣称：本轮整改降低了旧 paper 和旧日志回流的风险，但不能改变 Stage C 当前证据状态。Stage B 仍只能写平台可靠；Stage C 仍是 pilot / failure audit；等变性、update prediction 和训练加速仍是 blocked。
