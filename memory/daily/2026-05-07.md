# Daily Memory: 2026-05-07

## Entry 1 - Stage A/B Review And Literature Grounding

Completed work:

- Re-reviewed the project workflow requirements and current Stage A/B evidence.
- Added [`doc/StageAB_review_2026-05-07.md`](../StageAB_review_2026-05-07.md) to make the good results, bad results, overclaiming risks, and Stage C entry criteria explicit.
- Added a 2026-05-07 literature verification note to [`doc/StageA_literature_baseline.md`](../StageA_literature_baseline.md).
- Marked [`doc/StageB_observation_protocol.md`](../StageB_observation_protocol.md) as an early protocol superseded by the formal scratch reduced LIIF/SIREN Stage B record.
- Fixed `li2021learning` and added core arbitrary-scale SR / internal recurrence references in [`paper/refs/main.bib`](../../paper/refs/main.bib).

Validation:

- Ran `pytest -q tests/test_stage_c_geometry_response.py tests/test_stage_b_controls.py tests/test_check_outputs.py tests/test_trajectory_schema.py`.
- Result: `32 passed in 2.29s`.
- Ran a Stage B control-analysis smoke command with 20 controls to verify the tool still executes.

Evidence-backed conclusions:

- Stage A is adequate for Stage C pilot design but not yet paper-ready.
- Stage B platform reliability is established for current scratch SIREN and scratch reduced LIIF outputs.
- LIIF high PC1 is not a valid geometry-correspondence claim because it is close to the permuted-update control.
- Current confidence in the main idea comes from response-space Stage C pilot signals, not from PCA.

Unresolved risks:

- LIIF-EQ / SE-INR source, checkpoint, and buffer policy are still not clean enough for matched controls.
- The current Stage C signal is image/content dependent; bird remains a failure case.
- Bibliography needs a final style/field audit before paper use.

## Expert Review

Solid: Stage B is a reliable observation platform; Stage A now has a more honest literature baseline; the project has clear anti-overclaiming rules.

Weak: Literature coverage is still a minimal map, not a complete related-work section. Stage C evidence is encouraging but not cross-image stable.

Next step justified: yes. A read-only Stage C response-object split on existing scratch reduced LIIF outputs is the right next move.

Do not overclaim: do not present PC1, baby-only signal, or descriptor ablation as proof of a general geometry-response law.

## Entry 2 - Workflow Memory Rule And Stage C Response-Object Split

Completed work:

- Added a global Codex memory rule in `/home/admin/.codex/AGENTS.md`.
- Added project-local workflow requirements in [`doc/Project_workflow.md`](../Project_workflow.md).
- Created this daily memory file under `doc/daily_memory/`.
- Extended [`experiments/Phase1_FittingDynamics/analyze_stage_c_geometry_response.py`](../../experiments/Phase1_FittingDynamics/analyze_stage_c_geometry_response.py) with additional response modes:
  - `feature_final_delta`;
  - `feature_trajectory_delta`;
  - `coord_jacobian_final_delta`;
  - `coord_jacobian_trajectory_delta`.
- Added tests for response-object selection and feature/Jacobian response descriptors in [`tests/test_stage_c_geometry_response.py`](../../tests/test_stage_c_geometry_response.py).
- Updated [`doc/StageB_observation_log.md`](../StageB_observation_log.md) and [`doc/StageAB_review_2026-05-07.md`](../StageAB_review_2026-05-07.md) with response-object results and revised next-step guidance.

Analyses run:

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_geometry_response.py \
  --results_dir results/FittingDynamics_StageB \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_mode {trajectory_delta,final_delta,feature_trajectory_delta,coord_jacobian_final_delta} \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0
```

Key results:

| run | output trajectory | output final | feature trajectory | coord-Jacobian final |
|---|---:|---:|---:|---:|
| LIIF baby seed123 | 0.1318 | 0.0950 | 0.0854 | 0.0385 |
| LIIF baby seed42 | 0.0873 | 0.0848 | 0.0558 | 0.0288 |
| LIIF baby seed456 | 0.0761 | 0.0371 | 0.0546 | 0.0178 |
| LIIF bird seed42 | -0.0352 | -0.0140 | 0.0088 | 0.0098 |
| LIIF butterfly seed42 | 0.0388 | 0.0387 | 0.0302 | 0.0331 |

Evidence-backed conclusions:

- Baby keeps a positive geometry-response signal across all four response objects.
- Butterfly remains weak but positive across all four response objects.
- Bird failure is strongest in output response; feature response and coordinate-Jacobian response move bird to small positive effects.
- Coordinate-Jacobian final response is positive for all five runs, but the effect is small.

Unresolved risks:

- Coordinate-Jacobian is finite-difference function sensitivity, not a parameter-space decoder Jacobian.
- Bird remains too weak to call resolved.
- Head/woman are not yet covered by scratch reduced LIIF Stage B runs.
- Full trajectory Jacobian may be computationally expensive and should not be run until final-delta Jacobian stability is clearer.

## Expert Review

Solid: The analysis is still read-only and reconstructs raw full snapshots with PSNR consistency checks. The response-object split gives a real improvement in understanding where the signal appears.

Weak: The strongest evidence is still baby-heavy. The all-positive coordinate-Jacobian result is interesting but small, so it should be treated as a prioritization signal, not a claim.

Next step justified: yes. A small Set5 head/woman extension with output trajectory, feature trajectory, and coord-Jacobian final response is justified before any equivariance comparison.

Do not overclaim: do not say bird is solved; do not call finite-difference coordinate Jacobian a decoder parameter Jacobian; do not claim cross-image stability until head/woman or additional seeds support it.

## Entry 3 - Stage A Literature Patch And Set5 Head/Woman Extension

Completed work:

- Updated the global and project workflow memory rules were already in place; verified `/home/admin/.codex/AGENTS.md` and [`doc/Project_workflow.md`](../Project_workflow.md) specify dated daily memory plus `Expert Review`.
- Added a Stage A literature-baseline patch in [`doc/StageA_literature_baseline.md`](../StageA_literature_baseline.md) for A-LIIF, LINF, CLIT/LIT, DIIF, Thera, Continuous Optical Zooming, and DIIN as verified-but-not-yet-paper-final related work.
- Completed scratch reduced LIIF SR x4 runs for Set5 `head.png` and `woman.png`:
  - `results/FittingDynamics_StageB/LIIF_reduced_head_sr4_seed42`;
  - `results/FittingDynamics_StageB/LIIF_reduced_woman_sr4_seed42`.
- Updated [`doc/StageB_observation_log.md`](../StageB_observation_log.md) with head/woman training, checks, and Stage C response-object probe results.
- Updated [`doc/StageAB_review_2026-05-07.md`](../StageAB_review_2026-05-07.md) so the immediate next-step recommendation reflects the completed head/woman extension.

Commands and outcomes:

```bash
CUDA_VISIBLE_DEVICES=2 python experiments/Phase1_FittingDynamics/run.py \
  --model liif \
  --image Data/Set5/HR/head.png \
  --sr 4 \
  --seed 42 \
  --device cuda \
  --save_dir results/FittingDynamics_StageB/LIIF_reduced_head_sr4_seed42
```

Outcome: completed; final PSNR `31.5904`, final loss `0.000693`, scratch reduced LIIF, no pretrained checkpoint.

```bash
CUDA_VISIBLE_DEVICES=2 python experiments/Phase1_FittingDynamics/run.py \
  --model liif \
  --image Data/Set5/HR/woman.png \
  --sr 4 \
  --seed 42 \
  --device cuda \
  --save_dir results/FittingDynamics_StageB/LIIF_reduced_woman_sr4_seed42
```

Outcome: completed; final PSNR `29.7222`, final loss `0.001066`, scratch reduced LIIF, no pretrained checkpoint.

```bash
python experiments/Phase1_FittingDynamics/check_outputs.py \
  results/FittingDynamics_StageB/LIIF_reduced_head_sr4_seed42

python experiments/Phase1_FittingDynamics/check_outputs.py \
  results/FittingDynamics_StageB/LIIF_reduced_woman_sr4_seed42
```

Outcome: both passed trajectory schema, trajectory shapes, summary schema, analysis readiness, and summary consistency.

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_geometry_response.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_{head,woman}_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_mode {trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta} \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --format table
```

Key results, `effect_vs_shuffle`:

| run | output trajectory | feature trajectory | coord-Jacobian final |
|---|---:|---:|---:|
| LIIF head seed42 | -0.0067 | 0.0234 | 0.0049 |
| LIIF woman seed42 | 0.0676 | 0.0463 | 0.0229 |

Corresponding `shuffled_response_p_le`:

| run | output trajectory | feature trajectory | coord-Jacobian final |
|---|---:|---:|---:|
| LIIF head seed42 | 0.6965 | 0.0448 | 0.0647 |
| LIIF woman seed42 | 0.0050 | 0.0050 | 0.0050 |

Validation:

```bash
pytest -q tests/test_stage_c_geometry_response.py tests/test_stage_b_controls.py tests/test_check_outputs.py tests/test_trajectory_schema.py
```

Outcome: `35 passed in 2.22s`.

Evidence-backed conclusions:

- Stage A still needs a paper-grade related-work table, but the most obvious arbitrary-scale SR literature omissions are now recorded.
- Stage B platform reliability now extends to the two added Set5 scratch reduced LIIF outputs.
- Stage C pilot confidence improved because `woman` gives positive evidence outside baby.
- Stage C pilot also became more bounded because `head` does not support output-trajectory correspondence, while `bird` remains a stronger failure case.
- Feature and coordinate-Jacobian response objects remain worth prioritizing, but their effects are not large enough to justify a strong claim.

Unresolved risks and next steps:

- `paper/refs/main.bib` remains non-final and needs a full style/field audit before paper use.
- LIIF-EQ / SE-INR source and checkpoint policy remain unresolved; no matched equivariance claim should be made.
- The next scientific step should be a formal Stage C pilot log/table plus bird/head failure diagnosis, not a full Stage C expansion.
- Baby/woman need additional seeds before treating them as stable positive cases.

## Expert Review

Solid: head/woman are real scratch reduced LIIF runs, checked by the same output validator, and analyzed with the same read-only response probes. `woman` adds a genuine positive case beyond baby.

Weak: `head` weakens any broad output-response claim, and `bird` remains unresolved. The current signal is image/content dependent and response-object dependent.

Next step justified: yes, but narrowly. A formal Stage C pilot log and targeted failure analysis are justified; immediate equivariance comparison or training-speed claims are not.

Do not overclaim: do not say Set5 is solved, do not call coordinate-Jacobian effects strong, and do not treat seed42 single-image positives as cross-image stability.

## Entry 4 - Workflow Cleanup And Scientific Reassessment

完成范围：

- 重新读取 `/home/admin/.codex/AGENTS.md` 的全局工作流规则。
- 确认最新全局规则要求：长期协作规则只放全局；项目内只记录事实、进展、实验、结论和风险。
- 删除 [`doc/Project_workflow.md`](../Project_workflow.md)，不再在项目中保留通用工作流规则文件。
- 重新审视 Stage A/B/C pilot 对主研究主旨的支持力度，没有继续启动新实验。

运行过的命令与结果：

```bash
sed -n '1,260p' /home/admin/.codex/AGENTS.md
```

结果：全局规则已包含“工作流规则存放”“每日工作记忆”“科研协作立场”等长期规则。

```bash
test ! -e doc/Project_workflow.md && echo 'Project_workflow.md removed'
```

结果：`Project_workflow.md removed`。

证据支持的结论：

- 当前项目不应保留 `doc/Project_workflow.md` 这类项目级通用工作流规则文件；每日记忆文件足够记录本项目工作追踪。
- 针对研究主旨，现有结果只证明平台可靠，并显示某些图像和 response object 上存在几何邻近对应 response 邻近的迹象。
- 现有结果不支持“统一、跨图像稳定的 geometry-to-response law 已被捕捉到”。
- baby/woman 的正信号和 bird/head 的失败不能简单解释为“规律强弱不同”；更严谨的判断是：当前 descriptor、response object、模型适配过程或分析协议没有稳定捕捉到假设中的统一机制。

未解决风险与下一步：

- 暂不应继续扩大 Stage C 或引入等变性比较。
- 下一步应先做方法审计：检查 geometry descriptor 是否定义得太弱、response object 是否混入优化/拟合质量 confound、patch matching 是否需要内容分层、bird/head 是否由 aliasing/PSNR/纹理类型/局部非唯一性导致失败。
- 如果审计后仍无法解释 failure case，应降低主 claim，把研究问题从“统一规律”改成“在哪些条件下可观测到局部 correspondence”。

## 专家评审

可靠：工作流规则已回到全局，项目内只保留记忆和项目事实；删除 `Project_workflow.md` 是正确清理。Stage B 平台可靠这一点仍成立。

不足：现有 Stage C pilot 不能证明主旨中的统一规律；正例和失败例并存，且 Spearman 全局相关弱，说明当前方法至少不充分。

下一步是否合理：合理的下一步不是继续堆实验，而是做方法审计和 failure-case 解释，尤其是 bird/head。

防止过度宣称：不能说“规律已经存在只是部分图像不明显”；目前只能说“在部分图像和 response object 上观察到局部迹象，统一机制尚未被稳定测到”。

## Entry 5 - Memory Relocation And Reviewer-Style Stage C Failure Audit

完成范围：

- 按最新全局规则将本项目记忆文件从 `doc/daily_memory/2026-05-07.md` 移动到根目录 [`memory/daily/2026-05-07.md`](../../memory/daily/2026-05-07.md)。
- 删除旧的 `doc/daily_memory/` 空目录。
- 新增正式只读审计工具 [`experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py`](../../experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py)。
- 新增测试 [`tests/test_stage_c_failure_audit.py`](../../tests/test_stage_c_failure_audit.py)。
- 更新 [`doc/StageAB_review_2026-05-07.md`](../../doc/StageAB_review_2026-05-07.md) 和 [`doc/StageB_observation_log.md`](../../doc/StageB_observation_log.md)，把 Stage C pilot 结论降级为“有线索但必须先做方法审计”。

运行过的命令与结果：

```bash
mkdir -p memory/daily && \
mv doc/daily_memory/2026-05-07.md memory/daily/2026-05-07.md && \
rmdir doc/daily_memory
```

结果：记忆文件已移到根目录 `memory/daily/`。

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --results_dir results/FittingDynamics_StageB \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --format table
```

审计结果：

| image | seed | verdict | support modes | negative | tiny | mean effect |
|---|---:|---|---:|---:|---:|---:|
| baby | 123 | consistent_support | 3/3 | 0 | 0 | 0.0846 |
| baby | 42 | mixed_weak_support | 2/3 | 0 | 1 | 0.0574 |
| baby | 456 | mixed_weak_support | 2/3 | 0 | 1 | 0.0495 |
| woman | 42 | mixed_weak_support | 2/3 | 0 | 1 | 0.0453 |
| butterfly | 42 | mixed_weak_support | 2/3 | 0 | 0 | 0.0347 |
| bird | 42 | failure_or_unresolved | 0/3 | 1 | 2 | -0.0056 |
| head | 42 | failure_or_unresolved | 0/3 | 1 | 2 | 0.0069 |

```bash
pytest -q tests/test_stage_c_failure_audit.py \
  tests/test_stage_c_geometry_response.py \
  tests/test_stage_b_controls.py \
  tests/test_check_outputs.py \
  tests/test_trajectory_schema.py
```

结果：`39 passed in 2.24s`。

证据支持的结论：

- Stage B 平台可靠的结论不变。
- Stage C pilot 当前不能被称为“扎实进入下一阶段”：只有 `baby seed123` 三个 response object 都达到一致支持。
- baby 其他 seed、woman、butterfly 是 mixed/weak support，不是强结论。
- bird/head 是 failure 或 unresolved；其中 bird 有低 final PSNR confound，head final PSNR 很高但 output response 仍失败，说明失败不能只归因于拟合质量。
- coord-Jacobian 的小正效应经常被判为 `tiny`，不能作为强证据。

未解决风险与下一步：

- 暂不继续扩展 Stage C，不引入等变性比较，不做训练加速 claim。
- 下一步应做方法审计：descriptor 是否太弱、response object 是否混入优化或拟合质量 confound、patch matching 是否应做内容分层、平滑/弱纹理图像是否需要不同几何定义。
- 只有 failure audit 能解释 bird/head 后，才考虑更多 seed 或更昂贵的 Jacobian/等变性对照。

## 专家评审

可靠：新增 failure audit 工具是只读的，有测试覆盖，且把微小正效应、弱全局相关、失败图像明确暴露出来；这比继续堆实验更符合审稿标准。

不足：审计阈值本身是保守工程规则，不是最终统计理论；内容分层目前只是全图摘要，还没有定位到具体 patch 类型。

下一步是否合理：合理。应先解释 bird/head failure 和 tiny Jacobian effect，而不是继续扩大实验矩阵。

防止过度宣称：不能把 mixed_weak_support 写成稳定规律，不能把 p=0.005 但 effect 很小的结果当强证据，不能把 baby 的稳定性外推到 Set5 或任意图像。

## Entry 6 - Stage C Content-Stratified Failure Audit

完成范围：

- 扩展正式只读审计工具 [`experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py`](../../experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py)，新增 `--content_stratification`。
- 新增内容分层诊断：按 LR-up patch 平均梯度强度和 patch 方差分别做 low / mid / high tercile。
- 每个 stratum 内复用同一 geometry-neighbor vs shuffled-response control，避免另起不可追溯分析路径。
- 新增聚合摘要 `content_stratified_summary`；详细逐行结果默认只在 JSON 中保留，表格模式默认显示 reviewer summary。
- 更新测试 [`tests/test_stage_c_failure_audit.py`](../../tests/test_stage_c_failure_audit.py)，覆盖 tercile mask、样本不足处理、stratified aggregate。
- 更新 [`doc/StageB_observation_log.md`](../../doc/StageB_observation_log.md) 和 [`doc/StageAB_review_2026-05-07.md`](../../doc/StageAB_review_2026-05-07.md)，记录内容分层 failure audit 的证据和边界。

运行过的命令与结果：

```bash
pytest -q tests/test_stage_c_failure_audit.py \
  tests/test_stage_c_geometry_response.py \
  tests/test_stage_b_controls.py \
  tests/test_check_outputs.py \
  tests/test_trajectory_schema.py
```

结果：`42 passed in 2.28s`。

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --results_dir results/FittingDynamics_StageB \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --content_stratification \
  --format table
```

关键内容分层结果：

| image | seed | high-gradient verdict | high-gradient mean effect | high-variance verdict | high-variance mean effect |
|---|---:|---|---:|---|---:|
| baby | 42 | consistent stratum support | 0.0734 | consistent stratum support | 0.0620 |
| baby | 123 | consistent stratum support | 0.0913 | consistent stratum support | 0.0878 |
| baby | 456 | consistent stratum support | 0.0683 | consistent stratum support | 0.0577 |
| woman | 42 | consistent stratum support | 0.0611 | mixed stratum support | 0.0695 |
| butterfly | 42 | mixed stratum support | 0.0190 | mixed stratum support | 0.0319 |
| bird | 42 | no support or negative | -0.0213 | no support or negative | -0.0178 |
| head | 42 | no support or negative | 0.0026 | no support | 0.0056 |

证据支持的结论：

- baby 三个 seed 和 woman seed42 的正信号主要集中在 high-gradient / high-variance patch。
- 当前 probe 不是在全图均匀测到 geometry-response correspondence；更像高内容强度局部区域中的信号。
- bird/head failure 没有被高梯度或高方差分层救回，因此不能解释为“平滑区域稀释了真实规律”。
- head final PSNR 高但仍失败，说明 failure 不能只归因于 fitting quality。
- bird final PSNR 低仍是 confound，但高内容强度分层也失败，说明还需要检查 aliasing、非唯一匹配、语义结构或 response 定义本身。
- butterfly 仍是弱支持，不能和 baby/woman 放在同一证据等级。

未解决风险与下一步：

- 内容分层是诊断，不是新 claim 搜索；不能事后只挑 high-gradient/high-variance 子集写强结论。
- 当前 descriptor 可能对弱结构、重复纹理或语义相关但局部外观不相似的 patch 不充分。
- 下一步应做 patch-level failure 可视化或非唯一近邻诊断：查看 bird/head 中 geometry nearest neighbors 的具体位置、patch 内容、response 距离分布和是否存在 aliasing/重复纹理误匹配。
- 暂不引入 LIIF-EQ、pretrained checkpoint 或完整 Stage C 扩展。

## 专家评审

可靠：新增内容分层审计是只读的，有测试覆盖，使用同一批 scratch reduced LIIF 结果和同一 response 重建链路；高内容强度 patch 中 baby/woman 的支持和 bird/head 的失败都是真实可复现的当前证据。

不足：tercile 分层只是粗诊断，不是统计建模；`support` 阈值是保守审稿规则但不是最终理论标准；当前只分析 Set5 小图和有限 seed。

下一步是否合理：合理。现在最应该定位 bird/head 的 patch-level failure，而不是继续扩大数据矩阵或引入等变性。

防止过度宣称：不能说统一规律已被捕捉；只能说当前 probe 在部分图像的高内容强度区域发现局部 correspondence，同时在 bird/head 上明确失败。

## Entry 7 - Stage C Bird/Head Patch-Level Failure Diagnostics

完成范围：

- 继续扩展正式只读审计工具 [`experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py`](../../experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py)，新增 `--patch_failure_diagnostics`、`--patch_failure_top_n`、`--show_worst_patches` 和 `--output_json`。
- 新增 patch-level 诊断函数：row-wise response-distance percentile、geometry top-k candidate、spatial quadrant、raw response norm rank、worst patch selection、non-unique geometry-neighbor summary。
- 更新 [`tests/test_stage_c_failure_audit.py`](../../tests/test_stage_c_failure_audit.py)，覆盖 rank/tie 处理、spatial region、row-wise response percentile、geometry top-k 排序和合成错配 patch 识别。
- 保存 diagnostic 产物，不写入 StageB 正式结果目录：
  - `results/FittingDynamics_StageC_diagnostics/bird_head_patch_failure_diagnostic_2026-05-07.json`
  - `results/FittingDynamics_StageC_diagnostics/baby123_woman42_patch_failure_reference_2026-05-07.json`
- 更新 [`doc/StageB_observation_log.md`](../../doc/StageB_observation_log.md) 和 [`doc/StageAB_review_2026-05-07.md`](../../doc/StageAB_review_2026-05-07.md)，记录指标定义、产物路径、patch-level 表和审稿式解释。

运行过的命令与结果：

```bash
pytest -q tests/test_stage_c_failure_audit.py \
  tests/test_stage_c_geometry_response.py \
  tests/test_stage_b_controls.py \
  tests/test_check_outputs.py \
  tests/test_trajectory_schema.py
```

结果：`47 passed in 2.19s`。此前同一组测试也在实现后通过：`47 passed in 2.21s`、`47 passed in 2.32s`。

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_bird_sr4_seed42 \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_head_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --patch_failure_diagnostics \
  --patch_failure_top_n 10 \
  --format table \
  --output_json results/FittingDynamics_StageC_diagnostics/bird_head_patch_failure_diagnostic_2026-05-07.json
```

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_baby_sr4_seed123 \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_woman_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --patch_failure_diagnostics \
  --patch_failure_top_n 10 \
  --format table \
  --output_json results/FittingDynamics_StageC_diagnostics/baby123_woman42_patch_failure_reference_2026-05-07.json
```

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_bird_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 8 \
  --seed 0 \
  --patch_failure_diagnostics \
  --patch_failure_top_n 2 \
  --format json > /tmp/stage_c_failure_audit_patch_smoke.json && \
python -m json.tool /tmp/stage_c_failure_audit_patch_smoke.json >/dev/null && \
rm /tmp/stage_c_failure_audit_patch_smoke.json
```

结果：JSON smoke 通过，临时 `/tmp` 文件已清理。

关键结果：

| run | response | top1 response percentile mean | p90 | worst patch frac | nonunique geometry frac | worst+nonunique |
|---|---|---:|---:|---:|---:|---:|
| bird | output trajectory | 0.527 | 0.912 | 0.281 | 0.025 | 0.000 |
| bird | feature trajectory | 0.401 | 0.847 | 0.132 | 0.025 | 0.000 |
| bird | coord-Jacobian final | 0.402 | 0.833 | 0.132 | 0.025 | 0.008 |
| head | output trajectory | 0.456 | 0.865 | 0.190 | 0.000 | 0.000 |
| head | feature trajectory | 0.415 | 0.856 | 0.149 | 0.000 | 0.000 |
| head | coord-Jacobian final | 0.444 | 0.807 | 0.107 | 0.000 | 0.000 |
| baby seed123 | output trajectory | 0.252 | 0.613 | 0.041 | 0.008 | 0.000 |
| woman seed42 | output trajectory | 0.337 | 0.728 | 0.066 | 0.000 | 0.000 |

证据支持的结论：

- bird/head 的 failure 能在 patch 层定位：当前 `rgb_grad` geometry top-1 近邻常常不是 response 近邻，且该现象在 baby seed123 / woman seed42 参考正例中明显弱得多。
- 当前不支持“主要是 descriptor 距离下多个 geometry 近邻几乎等距”这一简单解释；`nonunique_geometry_frac` 很低，且严重错配与非唯一近邻的重叠接近 0。
- 这不排除语义层面的多义、重复纹理或 aliasing；它只说明当前欧氏 `rgb_grad` descriptor 的 top-k 非唯一性不是主要可见原因。
- 严重错配 patch 的 raw response norm rank 接近中位或略高，不像单纯由 response norm 极端异常造成。
- bird 的 output-trajectory 错配最重，并有 bottom-right 区域集中迹象；head 的错配更分散。head final PSNR 高，因此 failure 不能只归因于 fitting quality。

未解决风险与下一步：

- patch-level 诊断仍是定位工具，不是正式科学 claim；阈值 `top1_response_percentile >= 0.8` 与 `topk/top1 <= 1.05` 是保守启发式，不是最终统计标准。
- 当前诊断没有直接显示 patch 图像内容；若继续推进，应生成明确标注 diagnostic 的 patch crop grid 或表格，查看 worst rows 是否对应 aliasing、重复纹理、边界、局部外观相似但语义不同等情况。
- 下一步更合理的是检查 scale/aliasing-aware descriptor、结构化 patch matching 或 response object 定义，而不是扩展 full Stage C、引入 LIIF-EQ 或写训练加速 claim。

## 专家评审

可靠：新增诊断是只读、可复现、有测试覆盖的；bird/head 与 baby/woman 参考对照使用相同 scratch reduced LIIF 结果、相同 raw full snapshot response 重建链路和相同参数设置。

不足：诊断仍缺少实际 patch crop 可视化，无法直接判断错配是否来自 aliasing、重复纹理或语义结构；非唯一近邻只在当前 descriptor 欧氏距离意义下被检查。

下一步是否合理：合理。应先看最坏 patch 的实际图像内容和候选近邻，再决定是否改 descriptor 或 response object；不应把这个结果升级成跨图像机制结论。

防止过度宣称：不能说 bird/head failure 已被解释完；只能说当前证据排除了“简单 top-k 非唯一近邻”和“response norm 极端异常”作为主要可见解释，并更精确地定位了 failure 表现。

## Entry 8 - Review Correction And Patch Crop Diagnostics

完成范围：

- 以专业科研审阅口径复核上一轮 patch-level failure 定位，发现一个实质问题：上一轮 `nonunique_geometry_frac` 的解释过严，把“top-k 最远候选也接近 top-1”误当作“存在多个近邻候选接近 top-1”，会低估近邻多义风险。
- 修正 [`experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py`](../../experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py)：
  - `nonunique_geometry_frac` 现在表示 top-k 内至少两个候选与 top-1 geometry distance 在 1% 内的 strict near-tie 比例；
  - 新增 `top2_nonunique_geometry_frac`，表示 top2/top1 geometry distance `<=1.05` 的更宽松 near-tie 比例；
  - 新增 `geometry_top2_to_top1_ratio`、`geometry_top2_minus_top1` 等行级诊断；
  - 新增 diagnostic patch crop grid 生成：`--patch_crop_dir` 和 `--patch_crop_scale`。
- 更新 [`tests/test_stage_c_failure_audit.py`](../../tests/test_stage_c_failure_audit.py)，新增 near-tie summary 修正测试和 patch crop 渲染测试。
- 重新生成 bird/head diagnostic JSON，并生成 6 张 diagnostic PNG：
  - `results/FittingDynamics_StageC_diagnostics/bird_head_patch_failure_diagnostic_2026-05-07.json`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_bird_sr4_seed42_trajectory_delta_patch_crop_diagnostic.png`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_bird_sr4_seed42_feature_trajectory_delta_patch_crop_diagnostic.png`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_bird_sr4_seed42_coord_jacobian_final_delta_patch_crop_diagnostic.png`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_head_sr4_seed42_trajectory_delta_patch_crop_diagnostic.png`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_head_sr4_seed42_feature_trajectory_delta_patch_crop_diagnostic.png`
  - `results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07/LIIF_reduced_head_sr4_seed42_coord_jacobian_final_delta_patch_crop_diagnostic.png`
- 更新 [`doc/StageB_observation_log.md`](../../doc/StageB_observation_log.md) 和 [`doc/StageAB_review_2026-05-07.md`](../../doc/StageAB_review_2026-05-07.md)，把“非唯一近邻不是主因”的表述降级为更保守的结论。

运行过的命令与结果：

```bash
pytest -q tests/test_stage_c_failure_audit.py \
  tests/test_stage_c_geometry_response.py \
  tests/test_stage_b_controls.py \
  tests/test_check_outputs.py \
  tests/test_trajectory_schema.py
```

结果：`50 passed in 2.25s`。实现 patch crop 后同一组测试也通过：`50 passed in 2.24s`。

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_bird_sr4_seed42 \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_head_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --patch_failure_diagnostics \
  --patch_failure_top_n 10 \
  --format table \
  --output_json results/FittingDynamics_StageC_diagnostics/bird_head_patch_failure_diagnostic_2026-05-07.json \
  --patch_crop_dir results/FittingDynamics_StageC_diagnostics/patch_crops_2026-05-07 \
  --patch_crop_scale 14
```

```bash
python experiments/Phase1_FittingDynamics/analyze_stage_c_failure_audit.py \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_baby_sr4_seed123 \
  --result_dir results/FittingDynamics_StageB/LIIF_reduced_woman_sr4_seed42 \
  --device cpu \
  --geometry_source lr_up \
  --geometry_descriptor rgb_grad \
  --response_modes trajectory_delta,feature_trajectory_delta,coord_jacobian_final_delta \
  --patch_size 7 \
  --stride 4 \
  --k 5 \
  --min_spatial_distance 8 \
  --n_shuffles 200 \
  --seed 0 \
  --patch_failure_diagnostics \
  --patch_failure_top_n 10 \
  --format table \
  --output_json results/FittingDynamics_StageC_diagnostics/baby123_woman42_patch_failure_reference_2026-05-07.json
```

关键修正后结果：

| run | response | top1 pct mean | p90 | worst frac | strict ties frac | top2 close frac | worst+strict ties |
|---|---|---:|---:|---:|---:|---:|---:|
| bird | output trajectory | 0.527 | 0.912 | 0.281 | 0.066 | 0.306 | 0.008 |
| bird | feature trajectory | 0.401 | 0.847 | 0.132 | 0.066 | 0.306 | 0.008 |
| bird | coord-Jacobian final | 0.402 | 0.833 | 0.132 | 0.066 | 0.306 | 0.008 |
| head | output trajectory | 0.456 | 0.865 | 0.190 | 0.107 | 0.281 | 0.025 |
| head | feature trajectory | 0.415 | 0.856 | 0.149 | 0.107 | 0.281 | 0.017 |
| head | coord-Jacobian final | 0.444 | 0.807 | 0.107 | 0.107 | 0.281 | 0.025 |
| baby seed123 | output trajectory | 0.252 | 0.613 | 0.041 | 0.083 | 0.314 | 0.017 |
| woman seed42 | output trajectory | 0.337 | 0.728 | 0.066 | 0.058 | 0.248 | 0.000 |

证据支持的结论：

- 上一轮关于 `nonunique_geometry_frac` 的指标解释有问题，已修正并在文档中明确记录；这是必要纠错。
- bird/head 与 baby/woman 的主要差异仍是 response mismatch：bird/head 的 `top1_response_percentile_mean`、`p90` 和 `worst_response_frac` 明显更高。
- top2 close frac 在 bird/head 与 baby/woman 中都不低，说明“近邻候选接近”是普遍现象，不能单独解释 failure，也不能简单排除。
- 严重 response 错配与 strict ties 的重叠仍小，说明最坏错配不主要来自多个候选几乎完全等距。
- patch crop grid 目检显示，bird/head 最坏 output-trajectory 匹配常见“小 patch 色块/边缘相似，但上下文位置和内容结构明显不同”的情况；这支持当前局部 `rgb_grad` descriptor 缺少上下文或结构约束的解释，但还不是 aliasing 或语义机制的证明。

未解决风险与下一步：

- patch crop 仍是人工诊断图，不是定量统计；不能把目检观察写成正式结论。
- 需要进一步做可复现的上下文增强 descriptor 或结构化 matching 对照，检查是否能降低 bird/head 的 response mismatch，同时不破坏 baby/woman 正例。
- 暂不应引入等变性、pretrained checkpoint 或完整 Stage C；当前更重要的是修补 matching/descriptor 的可解释性。

## 专家评审

可靠：本轮主动发现并修正了上一轮诊断定义的过严问题，避免了“非唯一近邻不是主因”的过强结论；新指标和 patch crop 有测试覆盖，产物可复现。

不足：patch crop 目检只提供方法审计线索；strict ties 和 top2 close 仍是启发式，不是完整的近邻多义理论。

下一步是否合理：合理。下一步应做上下文增强 descriptor 或结构化 matching 的最小只读对照，作为方法修复尝试，而不是扩大实验规模。

防止过度宣称：不能说 bird/head failure 已解释完；只能说当前证据更指向“局部 descriptor 缺少上下文/结构约束”，且非唯一近邻风险需要更细定义后继续检查。
